---
story_id: "1.2"
epic_id: "EPIC-1"
title: "Build Wallpaper Mode Toggle (Fixed/Variable)"
status: "TODO"
priority: "HIGH"
points: 5
created_date: 2026-04-27
updated_date: 2026-04-27
---

# Story 1.2: Build Wallpaper Mode Toggle (Fixed/Variable)

## Summary
Implement wallpaper mode toggle allowing users to switch between Fixed mode (single static wallpaper) and Variable mode (scheduled or rotating wallpapers). This feature gates time-based scheduling and rotation systems.

## Acceptance Criteria

- [ ] Toggle UI: "Fixed Mode" ↔ "Variable Mode" switch in WallpaperPanel
- [ ] Fixed Mode: Single wallpaper applied and stays constant
- [ ] Variable Mode: Enables scheduling and rotation features (UI shows conditionally)
- [ ] Mode selection persisted to Zustand store and profile
- [ ] Switching modes does not disrupt current wallpaper
- [ ] Clear visual indication of active mode (highlight, icon, label)
- [ ] TypeScript types for mode: `type WallpaperMode = 'fixed' | 'variable'`
- [ ] Unit tests for mode switching logic

## Technical Approach

**Store Updates (`src/stores/wallpaperStore.ts`):**
- Add `mode: 'fixed' | 'variable'` to state
- Add action `setMode(mode: WallpaperMode)` with persist

**Component Updates (`src/components/WallpaperPanel.tsx`):**
- Add toggle switch/radio buttons for mode selection
- Conditionally render "Scheduling" and "Rotation" sections based on mode
- Visual styling to highlight active mode

**Service Updates (`src/services/wallpaperService.ts`):**
- Add `getCurrentMode()` → returns current mode
- Add `validateModeTransition(from, to)` → ensures valid transitions

## Deliverables

| Path | Description | Status |
|------|-------------|--------|
| `src/stores/wallpaperStore.ts` | Updated with mode state | TODO |
| `src/components/WallpaperPanel.tsx` | Updated with toggle UI | TODO |
| `src/services/wallpaperService.ts` | Updated with mode logic | TODO |
| `tests/wallpaperMode.test.ts` | Mode switching tests | TODO |

## File List
- [ ] `src/stores/wallpaperStore.ts` (updated)
- [ ] `src/components/WallpaperPanel.tsx` (updated)
- [ ] `src/services/wallpaperService.ts` (updated)
- [ ] `tests/wallpaperMode.test.ts`

## Dependencies

- EPIC-0 completed
- Story 1.1 completed (basic wallpaper selection)

## Success Metrics

- [ ] Toggle works smoothly with instant feedback
- [ ] Mode state persists across app restarts
- [ ] Variable mode UI elements appear/disappear correctly
- [ ] Test coverage >90%

## Definition of Done

- [ ] All acceptance criteria checked
- [ ] Toggle functional and styled
- [ ] Store updated and tested
- [ ] ESLint and TypeScript passing
- [ ] Unit tests passing
- [ ] Pushed to feature branch
