---
story_id: "1.3"
epic_id: "EPIC-1"
title: "Create Time-Based Wallpaper Scheduler"
status: "TODO"
priority: "HIGH"
points: 13
created_date: 2026-04-27
updated_date: 2026-04-27
---

# Story 1.3: Create Time-Based Wallpaper Scheduler

## Summary
Implement time-based wallpaper scheduling allowing users to create schedules where wallpapers change at specific times (30-minute granularity). Users can define multiple schedules and the system auto-transitions wallpapers.

## Acceptance Criteria

- [ ] UI: Schedule builder with time picker (30-minute granularity)
- [ ] Create multiple schedules: Start time → Wallpaper image
- [ ] Time picker: Native or custom widget supporting 00:00-23:59 format
- [ ] Auto-transition: System changes wallpaper at scheduled times (background service)
- [ ] Scheduler persists to store and profile (JSON serializable)
- [ ] Tolerance: Transitions within ±5 minutes of scheduled time
- [ ] Service: `scheduleWallpaper(time: string, imagePath: string)` method
- [ ] Service: Background job runs continuously checking scheduled times
- [ ] Error handling: Handle missing image files, invalid times gracefully
- [ ] Performance: <1% CPU during idle waiting for schedule
- [ ] TypeScript types for Schedule: `{ time: string; imagePath: string; enabled: boolean }`
- [ ] Unit tests for scheduler logic

## Technical Approach

**Components:**
- `src/components/ScheduleBuilder.tsx` — Time picker + wallpaper selector + list of schedules
- `src/components/TimePickerWidget.tsx` — Time input (30-min granularity)

**Services:**
- `src/services/wallpaperService.ts`:
  - `scheduleWallpaper(time: string, imagePath: string)` → Add schedule
  - `removeSchedule(scheduleId: string)` → Remove schedule
  - `getSchedules()` → Retrieve all schedules
  - `startScheduler()` → Begin background scheduler
  - `stopScheduler()` → Stop background scheduler

**Background Job:**
- Use `node-cron` or custom interval-based scheduler
- Every 1 minute: check if current time matches any schedule
- On match: apply wallpaper asynchronously
- Log transitions for debugging

**Store (`src/stores/wallpaperStore.ts`):**
- `schedules: Schedule[]`
- `currentScheduleId: string | null`

## Deliverables

| Path | Description | Status |
|------|-------------|--------|
| `src/components/ScheduleBuilder.tsx` | Schedule UI builder | TODO |
| `src/components/TimePickerWidget.tsx` | Time input widget | TODO |
| `src/services/wallpaperService.ts` | Scheduler implementation | TODO |
| `src/stores/wallpaperStore.ts` | Store updates for schedules | TODO |
| `tests/wallpaperScheduler.test.ts` | Scheduler logic tests | TODO |

## File List
- [ ] `src/components/ScheduleBuilder.tsx`
- [ ] `src/components/TimePickerWidget.tsx`
- [ ] `src/services/wallpaperService.ts` (updated)
- [ ] `src/stores/wallpaperStore.ts` (updated)
- [ ] `tests/wallpaperScheduler.test.ts`

## Dependencies

- EPIC-0 completed
- Story 1.1 and 1.2 completed
- `node-cron` library installed

## Technical Notes

1. **Time Format:** Use 24-hour format (HH:MM) with 30-minute granularity
2. **Background Scheduler:** Start in Electron main process for persistence across window close
3. **Performance:** Use native `setInterval()` rather than heavy job queue for simplicity
4. **Edge Cases:** Handle schedule at midnight, DST transitions, system time changes

## Success Metrics

- [ ] Wallpaper transitions at scheduled times within tolerance
- [ ] Multiple schedules work correctly
- [ ] Scheduler resumes after app restart
- [ ] CPU usage <1% during idle
- [ ] Test coverage >90%

## Definition of Done

- [ ] All acceptance criteria checked
- [ ] Schedule builder UI functional
- [ ] Scheduler runs in background
- [ ] ESLint and TypeScript passing
- [ ] Unit tests passing
- [ ] Pushed to feature branch
